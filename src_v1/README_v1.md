# P3 코드 v1 (초기 버전 — 보존용)

> v2(상위 폴더 `P3_code/`)가 정식이다. v1은 *기록/비교용*으로만 보존한다.
> v1 = 2026-06-15 처음 작성·샌드박스 검증한 파이프라인. v2가 이를 격상함.

## v1 → v2 차이 요약
| 항목 | v1 (이 폴더) | v2 (상위 폴더, 정식) |
|------|--------------|----------------------|
| 공 물리 | 0.05kg·반지름 0.05, spawn offset 0.05 | **0.15kg·반지름 0.07, offset 0.15**(겹침/너무쉬움 해결) |
| 과제 | 고정방향 직진 차기만 | + **목표조건부(조준)** |
| sim2real | 없음 | + **관측노이즈·액추에이터지연** |
| 보상 | 결과보상(공속도·전진) | + **최초접촉 1회 보너스**(anti-해킹 강화) |
| 해석적 차기 | 직진만 | + **hip_yaw 조준** |
| 추가 도구 | — | `analytic_tune.py`, `eval_sim2sim.py` |
| info 키 | `ball_x`, `ball_disp` | `proj`, `aim_err_deg`, `hit`, `ball_disp` |

## ⚠ 주의
- v1은 공이 가벼워 **랜덤 행동으로도 공이 멀리 가는(=과제가 너무 쉬운)** 한계가 있다. *결과 산출용으로는 v2를 쓸 것.*
- v1과 v2를 *동시에 한 폴더에서 실행하지 말 것*(파일명 충돌). 비교가 필요하면 각각 별도 폴더에서.

## 파일
`op3_kick_scene.xml` · `op3_kick_env.py` · `domain_randomization.py` · `analytic_kick.py` · `model_inspect.py` · `train.py` · `eval.py`
